datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Question {
  id    Int     @id @default(autoincrement())
  text  String
  answer  String
  category  String
  difficulty  String
  source  String
}
